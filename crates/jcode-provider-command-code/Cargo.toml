[package]
name = "jcode-provider-command-code"
version = "0.1.0"
edition = "2024"
description = "Command Code provider wire constants/config DTOs (leaf crate, no jcode dependencies)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
